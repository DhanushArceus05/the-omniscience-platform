# =====================================================================
# The Omniscience Platform — Environment Variables (Phase 0: Foundation)
# Copy this file to .env and fill in real values. Never commit .env.
# =====================================================================

# ---------- Global ----------
NODE_ENV=development
LOG_LEVEL=info

# ---------- apps/web (React + Vite) ----------
VITE_APP_NAME="The Omniscience Platform"
VITE_API_BASE_URL=http://localhost:4000
VITE_AI_SERVICE_BASE_URL=http://localhost:8000

# ---------- apps/api (NestJS) ----------
API_PORT=4000
API_HOST=0.0.0.0
API_CORS_ORIGIN=http://localhost:5173

# ---------- apps/ai-service (FastAPI) ----------
AI_SERVICE_PORT=8000
AI_SERVICE_HOST=0.0.0.0

# ---------- PostgreSQL ----------
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=omniscience
POSTGRES_USER=omniscience
POSTGRES_PASSWORD=changeme
POSTGRES_URL=postgresql://omniscience:changeme@localhost:5432/omniscience

# ---------- MongoDB ----------
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_DB=omniscience
MONGO_USER=omniscience
MONGO_PASSWORD=changeme
MONGO_URL=mongodb://omniscience:changeme@localhost:27017/omniscience?authSource=admin

# ---------- Redis ----------
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_URL=redis://localhost:6379

# ---------- Qdrant (Vector DB) ----------
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_URL=http://localhost:6333

# ---------- Reserved for future phases (do not use in Phase 0) ----------
# JWT_ACCESS_SECRET=
# JWT_REFRESH_SECRET=
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASSWORD=
# OBJECT_STORAGE_ENDPOINT=
# OBJECT_STORAGE_BUCKET=
